{

	"role": {

		"name": "aicoin.db.roles::cdsAdmin",

		"schema_privileges": [{
			"privileges": ["EXECUTE", "CREATE TEMPORARY TABLE", "INSERT", "UPDATE", "DELETE", "DROP", "REFERENCES", "SELECT", "SELECT METADATA", "SELECT CDS METADATA"]
		}]

	}
}